{
  "name": "ligaya-functions",
  "private": true,
  "version": "0.1.0",
  "description": "Cloud Functions for the Ligaya backend: family alert (push) fan-out on EMERGENCY_ACTIVE (LIGAYA_ARCHITECTURE_FINAL_VOICE.md section 17, implementation Step 18).",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo 'Run `npm run test:functions` from backend/ instead — firebase.json lives there, not here.' && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1"
  },
  "devDependencies": {
    "firebase-tools": "^13.23.1"
  }
}

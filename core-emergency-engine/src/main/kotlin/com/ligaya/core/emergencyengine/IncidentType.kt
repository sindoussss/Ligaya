package com.ligaya.core.emergencyengine

/** LIGAYA_ARCHITECTURE_FINAL_VOICE.md section 18's field set — the first formal definition of
 *  this vocabulary anywhere in the codebase (previously only shown in that section's mockup). */
enum class IncidentType {
    FIRE,
    POLICE,
    MEDICAL,
    RESCUE,
    OTHER,
}
